setInterval
===========

### What is `setInterval`?

`setInterval` is a method that allows you to repeatedly execute a function at a specified interval in milliseconds.

### Clearing `setInterval`

If you need to stop the repeated execution of a function scheduled with `setInterval`, you can use `clearInterval`.