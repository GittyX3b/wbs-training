setTimeout
==========

### What is `setTimeout`?

`setTimeout` is a method that allows you to execute a function once after a specified delay in milliseconds.

### Clearing `setTimeout`

If you need to cancel the execution of a function scheduled with `setTimeout`, you can use `clearTimeout`.